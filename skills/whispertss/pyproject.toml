[project]
name = "whispertss"
version = "0.1.0"
description = "OpenAI Whisper local speech recognition"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"